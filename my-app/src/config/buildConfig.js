// Este archivo se generará durante el build con GitHub Actions
export const clientPasscodes = {
  // Los hashes de los passcodes se insertarán aquí durante el build
  // NO EDITAR MANUALMENTE - Se genera automáticamente
};
